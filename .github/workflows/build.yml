name: Build LineageOS for Redmi Note 10 Pro

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Free Up Disk Space
      run: |
        echo "=== Disk space before cleanup ==="
        df -h
        
        # Remove pre-installed runtimes and heavy packages to reclaim ~75GB
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /opt/ghc
        sudo rm -rf /usr/local/.ghcup
        sudo rm -rf /usr/local/share/powershell
        sudo rm -rf /usr/share/swift
        sudo rm -rf /var/lib/docker
        sudo rm -rf /usr/local/lib/node_modules
        
        sudo apt-get clean
        sudo rm -rf /var/lib/apt/lists/*
        
        echo "=== Disk space after cleanup ==="
        df -h

    - name: Create 10GB Swap Space (OOM Prevention)
      run: |
        sudo fallocate -l 10G /swapfile || sudo dd if=/dev/zero of=/swapfile bs=1M count=10240
        sudo chmod 600 /swapfile
        sudo mkswap /swapfile
        sudo swapon /swapfile
        free -h

    - name: Install Build Dependencies
      run: |
        sudo dpkg --add-architecture i386
        
        # Retry loop for apt update to prevent transient mirror failures
        for i in {1..5}; do 
          sudo apt-get update && break || sleep 5
        done
        
        DEBIAN_FRONTEND=noninteractive sudo apt-get install -y --fix-missing \
          git-core gnupg flex bison build-essential zip curl zlib1g-dev \
          gcc-multilib g++-multilib libc6-dev-i386 libncurses5-dev \
          libx11-dev lib32z1-dev libgl1-mesa-dev \
          libxml2-utils xsltproc unzip fontconfig ccache python3 python3-pip \
          openjdk-11-jdk libssl-dev bc schedtool libtinfo5 rsync python-is-python3

    - name: Install Google Repo Tool
      run: |
        mkdir -p ~/bin
        curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
        chmod a+x ~/bin/repo
        echo "$HOME/bin" >> $GITHUB_PATH

    - name: Configure Git
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"

    - name: Set up CCache
      uses: actions/cache@v4
      with:
        path: ~/.ccache
        key: ccache-lineage-20.0-sweet-${{ github.sha }}
        restore-keys: |
          ccache-lineage-20.0-sweet-

    - name: Initialize LineageOS Source & Manifests
      run: |
        mkdir -p ~/rom
        cd ~/rom
        repo init -u https://github.com/LineageOS/android.git -b lineage-20.0 --depth=1
        
        # Flexibly locate and copy local manifests
        mkdir -p .repo/local_manifests
        if [ -d "$GITHUB_WORKSPACE/local_manifests" ]; then
          cp -r $GITHUB_WORKSPACE/local_manifests/* .repo/local_manifests/
        elif [ -f "$GITHUB_WORKSPACE/sweet.xml" ]; then
          cp $GITHUB_WORKSPACE/sweet.xml .repo/local_manifests/
        elif [ -f "$GITHUB_WORKSPACE/.repo/local_manifests/sweet.xml" ]; then
          cp $GITHUB_WORKSPACE/.repo/local_manifests/sweet.xml .repo/local_manifests/
        fi
        
        repo sync -c --no-clone-bundle --no-tags --optimized-fetch --prune -j$(nproc --all)

    - name: Build LineageOS ROM
      run: |
        cd ~/rom
        export CCACHE_EXEC=$(which ccache)
        export USE_CCACHE=1
        export CCACHE_DIR=~/.ccache
        ccache -M 15G
        ccache -z
        
        # Prevent Soong, Java Heap, & Ninja OOM Kills
        export _JAVA_OPTIONS="-Xmx4g"
        export SOONG_PARALLEL_BUILD=2
        export NINJA_ARGS="-j2"
        
        source build/envsetup.sh
        lunch lineage_sweet-userdebug
        
        # Compile with 2 threads to control memory usage
        mka bacon -j2
        
        ccache -s

    - name: Upload Output ROM Artifact
      uses: actions/upload-artifact@v4
      with:
        name: LineageOS-20-sweet
        path: ~/rom/out/target/product/sweet/*.zip
        if-no-files-found: error
