name: Build LineageOS for Redmi Note 10 Pro

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Free Up Disk Space
      run: |
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /opt/ghc
        sudo rm -rf /usr/local/.ghcup
        sudo apt-get clean
        sudo rm -rf /var/lib/apt/lists/*
        df -h

    - name: Install Build Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          git-core gnupg flex bison build-essential zip curl zlib1g-dev \
          gcc-multilib g++-multilib libc6-dev-i386 libncurses5-dev \
          x11proto-core-dev libx11-dev lib32z1-dev libgl1-mesa-dev \
          libxml2-utils xsltproc unzip fontconfig ccache python3 python3-pip \
          openjdk-11-jdk libssl-dev bc schedtool

    - name: Install Google Repo Tool
      run: |
        mkdir -p ~/bin
        curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
        chmod a+x ~/bin/repo
        echo "$HOME/bin" >> $GITHUB_PATH

    - name: Set up CCache
      uses: actions/cache@v4
      with:
        path: ~/.ccache
        key: ccache-lineage-20-sweet-${{ github.sha }}
        restore-keys: |
          ccache-lineage-20-sweet-

    - name: Initialize LineageOS Source & Manifests
      run: |
        mkdir -p ~/rom
        cd ~/rom
        repo init -u https://github.com/LineageOS/android.git -b lineage-20.0 --depth=1
        mkdir -p .repo/local_manifests
        cp $GITHUB_WORKSPACE/.repo/local_manifests/sweet.xml .repo/local_manifests/
        repo sync -c -j$(nproc --all) --force-sync --no-clone-bundle --no-tags

   - name: Build LineageOS ROM
      run: |
        cd ~/rom
        export CCACHE_EXEC=$(which ccache)
        export USE_CCACHE=1
        export CCACHE_DIR=~/.ccache
        ccache -M 10G
        ccache -z
        
        source build/envsetup.sh
        lunch lineage_sweet-userdebug
        mka bacon -j$(nproc --all)
        
        ccache -s

    - name: Upload Output ROM Artifact
      uses: actions/upload-artifact@v4
      with:
        name: LineageOS-20-sweet-GApps
        path: ~/rom/out/target/product/sweet/*.zip
